pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                script {
                    // Execute the deploy.sh script
                    sh "chmod +x deploy.sh"
                    sh './deploy.sh'
                }
            }
        }
    }
}

